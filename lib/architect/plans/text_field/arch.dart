

part of devspace;

class ArchTextField extends ArchBase<kTextFieldVariant, TextFieldData> {

  const ArchTextField({
    super.key,
    super.data = const TextFieldData(),
    super.variant = kTextFieldVariant.regular,
  });
  
}