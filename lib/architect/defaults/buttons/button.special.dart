


part of devspace;

class DefaultButtonSpecial extends DefaultStatelessWidget<ButtonData> {

  const DefaultButtonSpecial({
    super.key,
    required super.data
  });

  @override
  Widget build(BuildContext context)
  {
    throw UnimplementedError();
  }
}